# coding: utf-8

"""
    Rapidata Asset API

    The API for the Rapidata Asset service

    The version of the OpenAPI document: 2026.09.09.1810-c43fa9f
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from rapidata.api_client.models.faucet_input_binding_input import FaucetInputBindingInput
from pydantic import ValidationError
from rapidata.api_client.lazy_model import LazyValidatedModel
from typing import Optional, Set
from typing_extensions import Self

class IFaucetInputReplicateFaucetInput(LazyValidatedModel):
    """
    Configures a faucet that generates samples via the Replicate API.
    """ # noqa: E501
    t: StrictStr = Field(alias="_t")
    model_owner: StrictStr = Field(description="The owner of the hosted model (e.g. \"stability-ai\").", alias="modelOwner")
    model_name: StrictStr = Field(description="The model name (e.g. \"sdxl\").", alias="modelName")
    model_version: Optional[StrictStr] = Field(default=None, description="Optional pinned model version hash.", alias="modelVersion")
    inputs: Optional[Dict[str, FaucetInputBindingInput]] = Field(default=None, description="Model inputs keyed by parameter name (e.g. \"prompt\", \"aspect_ratio\"), each bound to a  literal value or to a prompt segment. An input not named here is not sent at all, with one  exception: the prompt input reads the benchmark's default text segment unless bound  otherwise. A reference image is one of these bindings like any other — bind the model's  image input to an asset segment to send one. num_outputs is supplied by the system  and must not appear. Bindings are validated against the model's live input schema when the  faucet is set.")
    additional_inputs: Optional[Dict[str, Any]] = Field(default=None, description="Legacy form of Inputs: literal values keyed by parameter name. A key present  in both is taken from Inputs.", alias="additionalInputs")
    __properties: ClassVar[List[str]] = ["_t", "modelOwner", "modelName", "modelVersion", "inputs", "additionalInputs"]

    @field_validator('t')
    def t_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['Replicate']):
            raise ValueError("must be one of enum values ('Replicate')")
        return value

    # model_config is inherited from LazyValidatedModel


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of IFaucetInputReplicateFaucetInput from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in inputs (dict)
        _field_dict = {}
        if self.inputs:
            for _key_inputs in self.inputs:
                if self.inputs[_key_inputs]:
                    _field_dict[_key_inputs] = self.inputs[_key_inputs].to_dict()
            _dict['inputs'] = _field_dict
        # set to None if model_version (nullable) is None
        # and model_fields_set contains the field
        if self.model_version is None and "model_version" in self.model_fields_set:
            _dict['modelVersion'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of IFaucetInputReplicateFaucetInput from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _data = {
            "_t": obj.get("_t"),
            "modelOwner": obj.get("modelOwner"),
            "modelName": obj.get("modelName"),
            "modelVersion": obj.get("modelVersion"),
            "inputs": dict(
                (_k, FaucetInputBindingInput.from_dict(_v))
                for _k, _v in obj["inputs"].items()
            )
            if obj.get("inputs") is not None
            else None,
            "additionalInputs": obj.get("additionalInputs")
        }
        try:
            _obj = cls.model_validate(_data)
        except ValidationError as _val_error:
            _obj = cls._lazy_construct(_data, _val_error)
        return _obj


