# coding: utf-8

"""
    Rapidata Asset API

    The API for the Rapidata Asset service

    The version of the OpenAPI document: 2026.07.24.1303-b3d1ec7
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from rapidata.api_client.models.i_workflow_evaluation_workflow import IWorkflowEvaluationWorkflow
from rapidata.api_client.models.i_workflow_grouped_ranking_workflow import IWorkflowGroupedRankingWorkflow
from rapidata.api_client.models.i_workflow_ranking_workflow import IWorkflowRankingWorkflow
from rapidata.api_client.models.i_workflow_simple_workflow import IWorkflowSimpleWorkflow
from pydantic import StrictStr, Field
from rapidata.api_client.lazy_model import LazyValidatedModel
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

IWORKFLOW_ONE_OF_SCHEMAS = ["IWorkflowEvaluationWorkflow", "IWorkflowGroupedRankingWorkflow", "IWorkflowRankingWorkflow", "IWorkflowSimpleWorkflow"]

class IWorkflow(LazyValidatedModel):
    """
    IWorkflow
    """
    # data type: IWorkflowEvaluationWorkflow
    oneof_schema_1_validator: Optional[IWorkflowEvaluationWorkflow] = None
    # data type: IWorkflowGroupedRankingWorkflow
    oneof_schema_2_validator: Optional[IWorkflowGroupedRankingWorkflow] = None
    # data type: IWorkflowRankingWorkflow
    oneof_schema_3_validator: Optional[IWorkflowRankingWorkflow] = None
    # data type: IWorkflowSimpleWorkflow
    oneof_schema_4_validator: Optional[IWorkflowSimpleWorkflow] = None
    actual_instance: Optional[Union[IWorkflowEvaluationWorkflow, IWorkflowGroupedRankingWorkflow, IWorkflowRankingWorkflow, IWorkflowSimpleWorkflow]] = None
    one_of_schemas: Set[str] = { "IWorkflowEvaluationWorkflow", "IWorkflowGroupedRankingWorkflow", "IWorkflowRankingWorkflow", "IWorkflowSimpleWorkflow" }

    # model_config is inherited from LazyValidatedModel


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = IWorkflow.model_construct()
        error_messages = []
        match = 0
        # validate data type: IWorkflowEvaluationWorkflow
        if not isinstance(v, IWorkflowEvaluationWorkflow):
            error_messages.append(f"Error! Input type `{type(v)}` is not `IWorkflowEvaluationWorkflow`")
        else:
            match += 1
        # validate data type: IWorkflowGroupedRankingWorkflow
        if not isinstance(v, IWorkflowGroupedRankingWorkflow):
            error_messages.append(f"Error! Input type `{type(v)}` is not `IWorkflowGroupedRankingWorkflow`")
        else:
            match += 1
        # validate data type: IWorkflowRankingWorkflow
        if not isinstance(v, IWorkflowRankingWorkflow):
            error_messages.append(f"Error! Input type `{type(v)}` is not `IWorkflowRankingWorkflow`")
        else:
            match += 1
        # validate data type: IWorkflowSimpleWorkflow
        if not isinstance(v, IWorkflowSimpleWorkflow):
            error_messages.append(f"Error! Input type `{type(v)}` is not `IWorkflowSimpleWorkflow`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in IWorkflow with oneOf schemas: IWorkflowEvaluationWorkflow, IWorkflowGroupedRankingWorkflow, IWorkflowRankingWorkflow, IWorkflowSimpleWorkflow. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in IWorkflow with oneOf schemas: IWorkflowEvaluationWorkflow, IWorkflowGroupedRankingWorkflow, IWorkflowRankingWorkflow, IWorkflowSimpleWorkflow. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into IWorkflowEvaluationWorkflow
        try:
            instance.actual_instance = IWorkflowEvaluationWorkflow.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into IWorkflowGroupedRankingWorkflow
        try:
            instance.actual_instance = IWorkflowGroupedRankingWorkflow.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into IWorkflowRankingWorkflow
        try:
            instance.actual_instance = IWorkflowRankingWorkflow.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into IWorkflowSimpleWorkflow
        try:
            instance.actual_instance = IWorkflowSimpleWorkflow.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into IWorkflow with oneOf schemas: IWorkflowEvaluationWorkflow, IWorkflowGroupedRankingWorkflow, IWorkflowRankingWorkflow, IWorkflowSimpleWorkflow. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into IWorkflow with oneOf schemas: IWorkflowEvaluationWorkflow, IWorkflowGroupedRankingWorkflow, IWorkflowRankingWorkflow, IWorkflowSimpleWorkflow. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], IWorkflowEvaluationWorkflow, IWorkflowGroupedRankingWorkflow, IWorkflowRankingWorkflow, IWorkflowSimpleWorkflow]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


